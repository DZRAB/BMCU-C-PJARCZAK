# BMCU-C 编译指南

本指南说明如何在本仓库编译 BMCU 固件。本工程**不使用 MounRiver Studio**，而是基于 **PlatformIO + 社区 CH32V 平台**（`Community-PIO-CH32V`）构建。所有编译变体都定义在 `platformio.ini` 中。

---

## 一、环境准备

只需准备好以下基础工具，编译器和 SDK 由 PlatformIO 首次编译时**自动下载**：

| 依赖 | 作用 | 是否自动安装 |
|------|------|--------------|
| **Python 3** | PlatformIO 运行环境 | 需自行安装 |
| **git** | 首次编译需从 GitHub 克隆 `Community-PIO-CH32V` 平台源码 | 需自行安装 |
| **PlatformIO (pio)** | 构建系统 | 需自行安装：`pip install platformio` |
| **联网** | 拉取平台包 + RISC-V GCC 工具链 + WCH noneos-sdk | 需联网 |
| **RISC-V GCC / WCH noneos-sdk** | 实际编译器 | ✅ 自动下载 |

> **Windows 用户提示**：在 **Git Bash / PowerShell / CMD** 中执行命令均可，只要 `pio` 和 `git` 在 PATH 中。若使用 **VSCode 的 PlatformIO 插件**，它自带 Python 和 pio，打开工程后直接点 Build 即可（仍需系统安装 git）。

安装验证：

```bash
python --version    # 或 python3 --version
git --version
pio --version
```

---

## 二、编译单个固件（最常用）

进入工程根目录，用 `-e` 指定一个构建变体：

```bash
pio run -e moj
```

- `moj`：单 BMCU（SOLO）固件，回抽 9.5 cm（`BAMBU_BUS_AMS_NUM=0`、`AMS_RETRACT_LEN=0.095f`）。
- 其他变体把 `moj` 换成对应的 `[env:xxx]` 名称即可，例如 `pio run -e ams_a_030`、`pio run -e ams_d_050`。

### 变体命名规则

`platformio.ini` 中每个 `[env:xxx]` 就是一个变体：

- **`moj`**：单模块（SOLO）固件，回抽长度固定 0.095f。
- **`ams_a_xxx` ~ `ams_d_xxx`**：`AMS_A`~`AMS_D` 四个槽位，后缀数字为回抽长度（米）：
  - `ams_a_030` → `BAMBU_BUS_AMS_NUM=0`，回抽 `0.30f`（30 cm）
  - `ams_b_010` → `BAMBU_BUS_AMS_NUM=1`，回抽 `0.10f`（10 cm）
  - …以此类推，回抽档位含：`010 020 025 030 035 040 045 050 055 060 065 070 075 080 085 090`（单位：米）
- **`fw`**：特殊环境，**从环境变量读取编译参数**，用于和一键脚本配合批量构建（见下文）。

> 注：单变体编译（如 `moj`、`ams_a_030`）只设置了槽位和回抽长度，**未开启** AUTOLOAD / RGB / P1S / SOFT_LOAD 等可选功能。需要这些功能请用 `fw` 环境手动传参。

### 单固件归档到 single_build/（build_one.sh）

上面 `pio run -e moj` 的产物只是 `.pio/build/<env>/firmware.bin`，**不分目录、不按功能命名**。若希望**单独编译一个固件、也像批量脚本那样分目录并命名成 `solo_0.095f.bin` / `ams_a_0.30f.bin`**，用封装脚本 `build_one.sh`（底层仍是 `fw` 环境传参 + 复制归档，命名规则与 `build_all_firmwares_softload.sh` 完全一致）。

> 注意：单固件产物放在独立的 **`single_build/`** 目录（**不是**批量专用的 `firmwares/`），两者都被 `.gitignore` 忽略、不入库；`single_build/` 用于单独补编，避免与全量构建产物混淆。

```bash
bash build_one.sh standard 1 1 SOLO     # single_build/standard(A1)/AUTOLOAD/FILAMENT_RGB_ON/SOLO/solo_0.095f.bin
bash build_one.sh softload 1 0 A 0.30   # single_build/soft_load(A1)/AUTOLOAD/FILAMENT_RGB_OFF/AMS_A/ams_a_0.30f.bin
bash build_one.sh p1s 0 1 D 0.80        # single_build/high_force_load(P1S)/NO_AUTOLOAD/FILAMENT_RGB_ON/AMS_D/ams_d_0.80f.bin
```

参数顺序：`<MODE> <AUTOLOAD> <RGB> <SLOT> <RETRACT>`，其中 `MODE` 为 `standard|p1s|softload`，`SLOT` 为 `SOLO|A|B|C|D`，`RETRACT` 为回抽长度（米，SOLO 可省略，默认 `0.095`）。该脚本**不会删除**现有 `single_build/`，只把单个固件放入对应位置，便于单独补编后并入 Releases 包。

---

## 三、用 `fw` 环境手动传参（完全自定义）

`[env:fw]` 从环境变量读取全部编译宏，可生成脚本未覆盖的组合（例如 `soft_load`）：

```bash
BAMBU_BUS_AMS_NUM=0 \
AMS_RETRACT_LEN=0.095f \
BMCU_DM_TWO_MICROSWITCH=1 \
BMCU_ONLINE_LED_FILAMENT_RGB=1 \
DBMCU_P1S=0 \
BMCU_SOFT_LOAD=0 \
pio run -e fw
```

各宏含义：

| 宏 | 取值 | 含义 |
|----|------|------|
| `BAMBU_BUS_AMS_NUM` | `0`~`3` | AMS 槽位编号，对应 A~D |
| `AMS_RETRACT_LEN` | 浮点（米） | filament 回抽长度，如 `0.095f` = 9.5 cm |
| `BMCU_DM_TWO_MICROSWITCH` | `0`/`1` | 双微动开关板 + AUTOLOAD（1 = 开启） |
| `BMCU_ONLINE_LED_FILAMENT_RGB` | `0`/`1` | 装入后 ONLINE LED 显示 filament RGB（1 = 开启） |
| `DBMCU_P1S` | `0`/`1` | P1S 模式（1 = 开启，高力加载） |
| `BMCU_SOFT_LOAD` | `0`/`1` | soft_load(A1) 软加载模式（1 = 开启） |

> **关于 `soft_load(A1)`**：当前 `build_all_firmwares.sh` 的矩阵只循环 `DBMCU_P1S=0/1`（即 `standard(A1)` 与 `high_force_load(P1S)` 两档），**不含** `BMCU_SOFT_LOAD`。因此一键脚本不会生成 `soft_load(A1)` 变体，需如上用 `fw` 环境单独编译（`BMCU_SOFT_LOAD=1`）。仓库现有的 `firmwares/soft_load(A1)/` 目录即作者用此方式单独生成并提交的。

---

## 四、一键编译全部固件

`build_all_firmwares.sh` 会遍历 **打印机模式 × AUTOLOAD × RGB × 槽位 × 回抽长度** 全组合，生成完整的 `firmwares/` 目录树（含 `manifest.txt` 校验清单）：

```bash
bash build_all_firmwares.sh
```

脚本行为说明：

1. 开头执行 `rm -rf firmwares` 后重建——**会删除现有 `firmwares/` 目录**再重新生成。
2. 生成的目录结构：
   ```
   firmwares/
   ├── which_to_choose.txt              (来自 which_to_choose_mode.txt)
   ├── standard(A1)/                    (DBMCU_P1S=0)
   │   └── AUTOLOAD/ / NO_AUTOLOAD/
   │       └── FILAMENT_RGB_ON/ / FILAMENT_RGB_OFF/
   │           └── SOLO/ AMS_A/ AMS_B/ AMS_C/ AMS_D/
   └── high_force_load(P1S)/            (DBMCU_P1S=1)
       └── ... (同上结构)
   ```
3. 每个子目录里的 `README.md` 由根目录对应的 `which_to_choose_*.txt` 复制并重命名而来（mode→顶层/模式目录，autoload→AUTOLOAD/NO_AUTOLOAD，filament_rgb→RGB 目录，slots→最里层）。
4. 末尾用 Python 生成 `firmwares/manifest.txt`（每行：`SHA256 CRC32 大小 相对路径`），用于校验固件完整性。

> **注意**：脚本不会复制 `firmwares/` 内的 `README.md`（那是作者手动添加的）。且脚本不含 `soft_load(A1)` 模式，运行后现有 `soft_load(A1)/` 目录会被删除。`firmwares/` 已被 `.gitignore` 忽略、不受 git 跟踪，误删只能重新运行脚本生成（或从 Releases 下载）。

### 含 soft_load(A1) 的扩展全量脚本

仓库另提供 `build_all_firmwares_softload.sh`，在原脚本基础上**额外编译 `soft_load(A1)` 模式**，使一键产物与现有 `firmwares/` 目录（含三种模式）完全一致：

```bash
bash build_all_firmwares_softload.sh
```

**与原脚本的区别：**

- 新增第三种模式 `soft_load(A1)`，编译参数为 `DBMCU_P1S=0` + `BMCU_SOFT_LOAD=1`（原脚本漏传 `BMCU_SOFT_LOAD`，故编不出 soft_load）。
- 各层 `README.md` 为对应 `which_to_choose_*.txt` 的改名副本（还原作者做法：现有 `firmwares/` 里的 `README.md` 实为选型指南 txt 改名，并非根目录主文档）。
- 三种模式格式统一：每个模式各层仅生成 `README.md`（不再额外生成 `which_to_choose.txt` 副本）。
- 同样在末尾生成 `firmwares/manifest.txt` 校验清单。
- 开头仍是 `rm -rf firmwares` 重建，运行前现有 `firmwares/` 会被覆盖。

**编译后会生成什么：**

- 编译矩阵：3 种模式 × AUTOLOAD/NO_AUTOLOAD × FILAMENT_RGB_ON/OFF ×（SOLO + AMS_A~D × 16 档回抽长度）= **约 780 个 `.bin`**（与现有 `firmwares/` 的 bin 数量一致）。
- 目录结构：`standard(A1)/`、`high_force_load(P1S)/`、`soft_load(A1)/` 三个模式目录，每个下再分 `AUTOLOAD/NO_AUTOLOAD` → `FILAMENT_RGB_ON/OFF` → `SOLO/AMS_A~D/`，最里层为 `solo_0.095f.bin`、`ams_a_0.30f.bin` 等。
- 文档文件：每个层级仅带 `README.md`（=对应选型指南中文版），三种模式格式统一。
- 校验文件：`firmwares/manifest.txt`。

> **耗时提醒**：脚本对 `fw` 环境执行约 780 次 `pio run`，且每次编译宏不同会触发全量重编；首次还需联网下载工具链。整体耗时很长（数十分钟到数小时），建议在后台运行，不要中途打断。

---

## 五、编译产物位置

### 单变体编译

产物位于：

```
.pio/build/<env名>/firmware.bin
```

例如 `pio run -e moj` 的产物为 `.pio/build/moj/firmware.bin`。同目录附带：

- `firmware.elf` — 带调试信息的可执行文件
- `firmware.map` — 内存映射（查看 RAM / Flash 占用）
- 其它中间产物视平台而定

> 若希望单固件也分目录并命名成 `solo_0.095f.bin` / `ams_a_0.30f.bin`，用 `build_one.sh`（见上文「单固件归档到 single_build/」），产物位于 `single_build/`，命名规则与一键脚本完全一致。

### 一键脚本编译

产物按模式/槽位/回抽长度分类放在 `firmwares/` 目录树中，例如：

```
firmwares/standard(A1)/AUTOLOAD/FILAMENT_RGB_ON/SOLO/solo_0.095f.bin
firmwares/high_force_load(P1S)/AUTOLOAD/FILAMENT_RGB_ON/AMS_A/ams_a_0.30f.bin
```

---

## 六、刷写固件

编译得到 `firmware.bin` 后，使用 **BMCU Flasher**（README 中有下载链接）在线或本地刷写即可，**无需 wchisptool**。

若希望用 PlatformIO 直接烧录（需 **WCH-Link 调试器 + 驱动**），可加 `-t upload`：

```bash
pio run -e moj -t upload
```

普通编译与刷写通常不需要调试器，用 BMCU Flasher 即可。

---

## 七、常见问题

**Q：第一次编译卡很久？**
A：正常。PlatformIO 正在后台下载 CH32V 平台包和 RISC-V GCC 工具链（需联网）。下载完成后后续编译很快。

**Q：提示找不到 `pio`？**
A：确认已 `pip install platformio` 且安装路径在 PATH 中；或使用 VSCode PlatformIO 插件。

**Q：提示找不到 `git`？**
A：安装 git 并确保可全局调用，平台包通过 git 从 GitHub 克隆。

**Q：想恢复被脚本误删的 `firmwares/`？**
A：`firmwares/` 已被 `.gitignore` 忽略、不受 git 跟踪，无法用 git 恢复；重新运行 `bash build_all_firmwares_softload.sh` 重新生成，或从 Releases 下载对应固件。

---

## 八、相关文档

- `README.md` — 固件用法、刷写、校准说明
- `BMCU开发说明.md` — 功能、软件架构、通讯协议、状态机、Flash 持久化
- `bmcu-vs-firmware-locks.md` — Bambu 固件更新对 BMCU 兼容性的影响
- `platformio.ini` — 所有编译变体与宏定义的权威来源

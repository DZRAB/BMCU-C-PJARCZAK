# BMCU-C 固件发布说明 —— v3.1-autoretract

> 适用机型：BMCU 切料机（STM32 版）
> 固件向打印机上报的版本号保持 `10.50`，兼容性不变，可直接替换旧版使用。

---

## 一、本次更新了什么

- **修复双开关自动回抽的一个问题**：上一版（v3.0）在部分退料场景下，filament 退到一半后不会自动往前送料就位，需要手动干预；本版已修复，退料完成后会自动接管送料，全程无需手动操作。
- **发布方式调整**：本次同时提供两个固件包，按你使用的「回抽方式」任选其一，详见下文。

---

## 二、两种回抽方式，先搞清楚

换料时 filament 要往回抽多少、抽到哪停，固件有两种做法：

| | 自动回抽（推荐默认） | 固定回抽长度 |
|---|---|---|
| 怎么判定料停在哪 | 由板上的**第二个微动开关 S2 自动判定**，退到料根即停 | 由你按 PTFE 管实际长度选一个固定值，退到该长度即停 |
| 需要量管子吗 | **不需要**，直接刷对应板型的固件即可 | 需要按 PTFE 长度从档位里挑一个 |
| 适用板型 | 仅双微动开关板（板上有 S1、S2 两个开关） | 全部板型（单开关板本就如此；双开关板也可选） |
| 固件文件名特征 | 带 `_auto` 后缀，如 `ams_a_2.00f_auto.bin` | 不带 `_auto`，如 `ams_a_0.30f.bin` |

**怎么选**：
- 你的板是**双微动开关板**，且 S2 工作正常 → 用**自动回抽包**，最省心，不用量管子。
- 你的板是**单开关板**（只有一个微动开关）→ 只能用固定回抽长度，两个包里都含单开关固件，挑哪个包都行，按包里的固定长度档位选。
- 你的双开关板 **S2 不可靠**，或你更习惯固定长度 → 用**固定回抽长度包**。

---

## 三、两个发布包说明

### 包 1：自动回抽长度包（`firmwares-auto`）

- 双微动开关板：每槽 1 个自动回抽固件（`_auto` 后缀），共 30 个。
- 单开关板：全回抽长度矩阵（SOLO 固定 9.5cm；AMS_A~D 支持 0.10~2.00 米，步长 5cm，共 39 档），共 942 个。
- **适合**：绝大多数双开关板用户（不想量 PTFE 长度）。

### 包 2：固定回抽长度包（`firmwares-fixed`）

- 所有板型均按固定回抽长度编译：双开关板同样出 39 档固定长度矩阵，与单开关板一致。
- 双开关板 942 个 + 单开关板 942 个，共 1884 个。
- **适合**：S2 不可靠的双开关板、或坚持使用固定回抽长度的用户。

> 两个包里的**单开关板固件内容一致**，区别只在双开关板那部分是自动回抽还是固定长度。

---

## 四、怎么选具体固件（刷哪一个）

无论用哪个包，选固件都看两点：**板型** 和 **PTFE 长度（仅固定回抽长度需要）**。

1. **确认板型**：看板上微动开关数量——两个开关 = 双微动开关板（AUTOLOAD）；一个开关 = 单开关板（NO_AUTOLOAD）。
2. **确认打印机模式**：standard / p1s / soft_load(A1)，对应目录已分好。
3. **固定回抽长度时按 PTFE 选档**：回抽长度从打印机内 AMS 分线器末端起算，加一点余量让 filament 清过分线器。例如到分线器约 9cm，选 9.5cm 档；P1 配置 PTFE 更长，通常需更长的档位。详细选型表见仓库 `which_to_choose_slots.txt` 与 `which_to_choose_autoload.txt`。

---

## 五、刷写与兼容性

- 刷写工具用 **WCHISPTool**（不要用 BMCU Flasher，本仓库固件用它刷会失败）。
- 固件上报版本保持 `10.50`，打印机兼容性不变。
- 打印机固件建议不高于 **1.07.00**。

---

## 六、文件清单速查

| 你想做的事 | 用哪个包 | 选固件依据 |
|---|---|---|
| 双开关板，不想量管子 | 自动回抽长度包 | 板型 + 打印机模式，挑 `_auto` 固件 |
| 单开关板 | 任意包 | 板型 + 打印机模式 + PTFE 长度档位 |
| 双开关板但 S2 不可靠 / 想固定长度 | 固定回抽长度包 | 板型 + 打印机模式 + PTFE 长度档位 |

更详细的使用与选型说明请见仓库 `docs/BMCU使用指南.md`；编译相关见 `docs/编译指南.md`。
